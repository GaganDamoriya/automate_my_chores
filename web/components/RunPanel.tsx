// Deprecated: the one-shot "detect & run" panel was replaced by persistent
// Automations (see app/automations). Kept as a no-op to avoid a dead import.
export function RunPanel() {
  return null;
}
